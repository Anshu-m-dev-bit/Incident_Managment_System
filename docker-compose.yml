version: '3.8'

services:

  app:
    build: .
    environment:
      DB_HOST: database-1.c9isuqawg6tv.eu-north-1.rds.amazonaws.com
      DB_USER: admin
      DB_PASSWORD: Forgot247
      DB_NAME: pypro
    expose:
      - "8000"

  nginx:
    image: nginx:latest
    ports:
      - "5000:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app